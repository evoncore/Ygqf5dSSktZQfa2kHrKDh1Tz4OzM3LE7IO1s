<li>
  <span class="content"><%= micropost.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  </span>
  <span>
    <% if current_user?(micropost.user) %>
      <%= link_to "", micropost, method: :delete, class: 'fa fa-times' %>
    <% end %>
  </span>
</li>