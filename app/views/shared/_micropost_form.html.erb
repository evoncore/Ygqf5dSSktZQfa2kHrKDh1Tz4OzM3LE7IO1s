<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.text_area :content, placeholder: "Compose new micropost...", class: 'send-post-item' %>
  <label class="btn fa fa-share"><%= f.submit "" %></label>
<% end %>